#!/bin/sh
# location of the php binary
if [ ! "$1" = "" ] ; then
	CRONSCRIPT=$1
else
	CRONSCRIPT=cron.php
fi

PHP_BIN=`which php`

if  ! ps auxwww | grep " $CRONSCRIPT" | grep -v grep | grep -v "magento-cron" 1>/dev/null 2>/dev/null ; then
	$PHP_BIN $CRONSCRIPT &
fi
